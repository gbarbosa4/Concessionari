
<html>
<head>
<title>Concessionari app</title>
</head>
<body>

	<div class="logo">
		<h1>Concessionari</h1>
	</div>

	<div class="bodyContent">
		 <h2>User Login</h2>
        {% if form.has_errors %}
            <p>Error validant usuari.</p>
        {% endif %}
        <form method="post" action=".">
            <p><label for="id_username">User:</label>
            {{ form.username }}</p>
            <p><label for="id_password">Pass:</label>
            {{ form.password }}</p>
            <p><label for="id_first_name">Pass:</label>
            {{ form.first_name }}</p>
            <p><label for="id_dni">Pass:</label>
            {{ form.dni }}</p>
            <p><label for="id_email">Pass:</label>
            {{ form.email }}</p>
            <input type="hidden" name="next" value="/mainpage" />
            <input type="submit" value="Save" />
            {% csrf_token %}
        </form>
	</div>

</body>
</html>
